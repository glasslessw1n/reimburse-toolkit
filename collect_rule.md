# 报销票据文件归类和命名规则

供智能体收取邮件附件后自动保存、归类和命名使用。

---

## 一、目录结构

```
bills/yyyyMM-yyMM/                          # 按报销月份归档，如 bills/202603-04/
├── MMDD-MMDD 城市1 城市2 .../               # 差旅行程：日期范围 + 途经城市
│   ├── MMDD 出发-到达 登机牌.pdf            # 登机牌
│   ├── MMDD 出发-到达 金额.pdf              # 火车票/机票行程单
│   ├── MMDD-MMDD 城市住宿水单.pdf/.jpg      # 酒店水单
│   ├── MMDD-MMDD 城市住宿发票.pdf           # 酒店发票
│   ├── 滴滴出行行程报销单X.pdf              # X = A/B/C/...
│   ├── 滴滴电子发票X.pdf                    # 与行程单字母一一对应
│   └── MMDD 餐饮发票 金额.pdf               # 餐饮发票
├── MMDD-MMDD 目的地1 目的地2 .../            # 自驾车行程（目录命名规则相同）
│   ├── 高速路行程单 出发地-出差地.pdf        # 高速路行程单
│   ├── MMDD 加油费 金额.pdf                 # 加油费发票
│   ├── MMDD 通行费NN 金额.pdf               # 通行费发票，NN=01/02/03
│   └── MMDD-MMDD 城市住宿水单/发票.pdf       # 如有住宿
├── 本地/                                    # 非差旅消费
│   ├── MM 通信发票 金额.pdf                 # 月度通信费
│   ├── MMDD 餐饮发票 金额.pdf               # 本地餐饮
│   ├── 滴滴出行行程报销单X.pdf
│   └── 滴滴电子发票X.pdf
└── ...
```

---

## 二、识别规则（按附件内容特征判断类型）

### 智能体执行顺序：从上到下匹配，命中即停止

| 优先级 | 判断条件（内容关键词） | 类型 | 子类 | 后缀提取 |
|--------|----------------------|------|------|---------|
| 1 | 含"高速路" + "行程单" | 自驾车行程单 | 高速路 | -- |
| 2 | 含"加油费" / "加油" | 加油费发票 | 自驾车 | -- |
| 3 | 含"通行费" / "ETC" / "高速通行" | 通行费发票 | 自驾车 | 提取NN序号 |
| 4 | 含"登机牌" / "BOARDING PASS" | 交通票 | 登机牌 | -- |
| 5 | 含"火车票" / "RAILWAY TICKET" / "电子客票" | 交通票 | 火车票 | -- |
| 6 | 含"航空运输电子客票行程单" / "机票" | 交通票 | 机票行程单 | -- |
| 7 | 含"住宿" + ("水单" / "账单" / "folio" / "guest receipt") | 住宿水单 | 酒店 | -- |
| 8 | 含"住宿" + ("发票" / "增值税" / "invoice") | 住宿发票 | 酒店 | -- |
| 9 | 含"滴滴出行" + ("行程报销单" / "行程单" / "trip") | 滴滴行程单 | -- | 提取字母后缀 |
| 10 | 含"滴滴" + ("电子发票" / "增值税电子普通发票") | 滴滴发票 | -- | 提取字母后缀 |
| 11 | 含"餐饮" / "餐费" / "餐饮服务" | 餐饮发票 | -- | -- |
| 12 | 含"通信" / "电信" / "话费" / "通讯" | 通信发票 | -- | -- |
| 13 | 含"增值税" / "发票" + 购买方名称 | 发票(通用) | -- | 按内容进一步判断 |

### 补充判断逻辑

- **自驾车行程单 vs 通行费**：行程单有"行程单"/起止地/行驶路线；通行费有"通行费"/ETC/入口出口收费站
- **加油费 vs 通行费**：加油费有"加油站"/"汽油"/"柴油"/油品号；通行费有"收费站"/"ETC"/"通行路段"
- **登机牌 vs 机票行程单**：登机牌有 boarding pass/登机牌字样；机票行程单有"电子客票行程单"/票价/燃油附加费
- **水单 vs 发票**：水单无税号/无发票代码；发票有发票代码/发票号码/税号
- **滴滴行程单 vs 滴滴发票**：行程单有"行程"/trip/起止时间/公里数；发票有"发票"/invoice/税率/税额

---

## 三、命名规则

### 3.1 交通票

```
登机牌: {MMDD} {出发城市}-{到达城市} 登机牌.pdf
火车票: {MMDD} {出发城市}-{到达城市} {金额}.pdf
机票行程单: {MMDD} {出发城市}-{到达城市} {金额}.pdf
```

- MMDD：从附件内容中提取的航班/车次日期
- 金额：从内容提取，只取数字（不含货币符号），如 `333`
- 城市名：取中文名，如 `重庆`、`广州`

### 3.2 酒店

```
水单: {入住MMDD}-{退房MMDD} {城市}住宿水单.{jpg|pdf}
发票: {入住MMDD}-{退房MMDD} {城市}住宿发票.pdf
```

- 日期范围：从入住日期到退房日期
- 城市：酒店所在城市
- 水单保留原始格式（jpg或pdf），发票统一pdf

### 3.3 滴滴

```
行程单: 滴滴出行行程报销单{字母}.pdf
发票:   滴滴电子发票{字母}.pdf
```

- 字母：A, B, C... 按邮件接收时间/行程时间排序分配
- 同一行程的行程单和发票用相同字母

### 3.4 自驾车出差

自驾车出差分三块，目录命名规则与其他行程相同（`MMDD-MMDD 目的地1 目的地2 ...`）。

```
高速路行程单: 高速路行程单 {出发地}-{出差地}.pdf    例: 高速路行程单 重庆-涪陵.pdf
加油费:      {MMDD} 加油费 {金额}.pdf             例: 0822 加油费 300.pdf
通行费:      {MMDD} 通行费{NN} {金额}.pdf         例: 0822 通行费01 8.94.pdf
```

- 出发地/出差地：从行程单内容提取，格式 `出发地-出差地`，如 `重庆-涪陵`
- 金额：从内容提取，只取数字（不含货币符号），可含小数，如 `300`、`8.94`、`13.7`
- 加油费：`{MMDD}` 为加油日期，`{金额}` 前有空格
- 通行费：`{MMDD}` 为通行日期，`{NN}` 为序号（01、02、03...），同日多张时按顺序编号；`{NN}` 与 `{金额}` 之间、`{金额}` 前后均保留空格
- 自驾车目录中通常无登机牌/火车票，行程单替代交通票作为行程证明

### 3.5 餐饮

```
{MMDD} 餐饮发票 {金额}.pdf
```

### 3.6 通信

```
{MM} 通信发票 {金额}.pdf
```

- MM：月份编号，如 `03` 代表3月

---

## 四、存放目录判断

### 行程 vs 本地判断

| 条件 | 存放位置 |
|------|---------|
| 有配套交通票（登机牌+火车票/机票） | `MMDD-MMDD 城市1 城市2 ...` 行程目录 |
| 有自驾车行程单（高速路行程单） | `MMDD-MMDD 目的地1 目的地2 ...` 自驾车行程目录 |
| 有加油费/通行费发票 | 对应自驾车行程目录（与行程单同目录） |
| 有酒店水单/发票 + 有交通票 | 行程目录（与交通票同目录） |
| 仅滴滴、餐饮、通信，无交通票 | `本地/` 目录 |
| 滴滴行程单中有明显的跨城行程 | 对应行程目录 |

### 行程目录命名

**普通差旅行程** — 从该行程的交通票中提取：

```
{首日MMDD}-{末日MMDD} {出发城市1} {途经城市2} {...} {最终返回城市}
```

- 首日：该行程第一张交通票的日期
- 末日：该行程最后一张交通票的日期
- 城市序列：按交通票的出发-到达顺序拼接，去重连续相同城市

**自驾车行程** — 目录命名规则与其他行程相同：

```
{首日MMDD}-{末日MMDD} {出发地城市} {出差地城市}
```

- 首日/末日：从行程单、加油费、通行费的日期范围提取
- 城市序列：从行程单的 `出发地-出差地` 提取，如 `0822-0823 涪陵`

---

## 五、执行流程（智能体用）

```
收到邮件附件 →
  1. 打开附件，读取内容（PDF文本提取 / OCR图片）
  2. 按"识别规则"判断类型
  3. 从内容中提取：日期、城市、金额、行程信息
  4. 按"命名规则"生成文件名
  5. 判断归属：已有行程目录 or 新建行程目录 or 本地目录
  6. 保存文件到对应目录
  7. 记录操作日志
```

### 日志格式

```
[2026-05-22 10:30] 滴滴电子发票A.pdf → 0303-0306 北京 广州/滴滴电子发票A.pdf  (金额: ¥45.20)
[2026-05-22 10:31] 0304 广州-重庆 338.pdf → 0303-0306 北京 广州/0304 广州-重庆 338.pdf  (金额: ¥338)
```

---

## 六、配置参数（可自定义）

```yaml
归档根目录: /path/to/bills/
月份归档: true              # 自动创建 yyyyMM-yyMM 目录
金额提取: 文件名后缀        # 金额写入文件名
配对检查: true              # 酒店水单=发票、滴滴行程单=发票
允许格式: [pdf, jpg, jpeg, png]
OCR语言: chi_sim            # 图片OCR识别语言
```

---

## 七、边缘情况处理

| 情况 | 处理方式 |
|------|---------|
| 无法识别类型 | 标记为 `其他/` 目录，保留原始文件名 |
| 金额无法提取 | 金额位置空缺，如 `0306 餐饮发票 .pdf` |
| 日期无法提取 | 用邮件接收日期填充 |
| 滴滴字母冲突 | 自动递增到下一个可用字母 |
| 同名文件已存在 | 追加 `_2` 后缀，或提示用户 |
| 多页PDF附件 | 保持完整，不拆分 |
| 图片格式附件 | JPG 统一保留原格式（水单常用），PNG 转 JPG |
